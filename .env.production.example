# T191/T202: Neon + Railway Environment Variables Configuration Template
# (Copy this file to .env.production and fill in your Neon/Railway details)
# ⚠️ DO NOT COMMIT THIS FILE - Add to .gitignore!

# ========================
# DATABASE (Neon PostgreSQL)
# ========================

# T191: Neon connection (pooled, for Railway use)
# Get from: Neon console → Connect → Connection string (with -pooler suffix)
DATABASE_URL="postgresql://[user]:[password]@pg[xxxx]-pooler.neon.tech:5432/exam_app_prod?sslmode=require"

# ========================
# API CONFIGURATION
# ========================

NODE_ENV="production"
PORT="3000"

# JWT Secret (generate: openssl rand -base64 32)
JWT_SECRET="[generate-secure-random-string-here]"

# ========================
# GOOGLE OAUTH (from Google Cloud Console)
# ========================

# From: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID (Web application type)
GOOGLE_CLIENT_ID="[from-google-console.apps.googleusercontent.com]"
GOOGLE_CLIENT_SECRET="[from-google-console]"

# Redirect URI configured in Google Console:
# http://localhost:3000/api/auth/callback (development)
# https://api-[railway-slug].railway.app/api/auth/callback (production)

# ========================
# PLAY INTEGRITY API (from Google Play Console)
# ========================

# Get from: Google Play Console → Setup → API Access
# Service Account JSON key file (base64 encoded or stored separately)
PLAY_INTEGRITY_CREDENTIALS='{"type":"service_account","project_id":"[...]","private_key":"[...]"}'

# Or if using file-based credentials (T191 setup guide):
# PLAY_INTEGRITY_CREDENTIALS_FILE="/path/to/service-account.json"

# ========================
# EXAM TYPE CONFIGURATION
# ========================

# Exam type ID for this deployment
# AWS CCP exam: exam-type-id-1
# SAA exam: exam-type-id-2
# (Hardcoded in mobile app; mobile app only sends requests for matching exam type)
EXAM_TYPE_ID="exam-type-id-1"

# ========================
# RAILWAY SPECIFIC (Auto-configured, shown for reference)
# ========================

# Railway assigns these automatically:
# RAILWAY_ENVIRONMENT_NAME="production"
# RAILWAY_DEPLOYMENT_ID="[auto-generated]"
# RAILWAY_SERVICE_ID="[auto-generated]"

# ========================
# OPTIONAL: MONITORING/LOGGING
# ========================

# (Out of scope for Phase 3, but template for future use)
# SENTRY_DSN="https://[key]@sentry.io/[project]" # Error tracking
# LOG_LEVEL="info" # or "debug" for verbose logs

# ========================
# NEON SPECIFIC (Reference)
# ========================

# Neon project URL (for monitoring):
# NEON_PROJECT_URL="https://console.neon.tech/app/projects/[project-id]"

# IMPORTANT: This file is for local development and CI/CD
# For Railway deployment, use Railway's environment variables dashboard:
# 1. Go to Railway console
# 2. Select exam-app-prod service
# 3. Go to Settings → Variables
# 4. Add each variable from above
# 5. Press Deploy to apply changes
